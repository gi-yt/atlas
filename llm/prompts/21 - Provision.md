I want to run Atlas on my development Server.

- Do not worry about provisioning with API
- Server has a IPv4, IPv6 address attached to the host
- Server also has an extra IPv6 subnet provisioned
- SSH keys are placed on the Server (root@ipv4) 
- Server is running Ubuntu 26.04

Update the spec to allow such provisioning.

If these changes are large then prepare a plan. If they are small then implement right away

Refer to frappe-dev skills for Frappe Framework and frappe from the references directory for Frappe Framework-related code.

Run unit tests in the end.

Update the specs.