Add more features to VM. Snapshot. Create new from Snapshot. Restore from Snapshot (Change the Base Image and Data. Keep Name, IP, Keys, etc). Resize (If stopped, ask to stop the machine if started). Pause. Rebuild (Wipe stored data, ask for base image, default to current).

---

We've implmented a lot of new actions on a couple of DocTypes. Look at the Desk UI recommendations for buttons.

---

Review state/plan and state/review. If everything is addressed. Delete those.

Commit the changes.
